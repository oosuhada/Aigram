/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "images.unsplash.com",
      "images.pexels.com",
      "picsum.photos",
    ],
  },
};

export default nextConfig;
